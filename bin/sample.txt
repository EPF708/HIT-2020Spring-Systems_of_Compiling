int num = 0x2f;
int step = 12.; 
while (num != 2.396e+22) {
  /* this is a comment.
   * test!
   * it's a little bit difficult. 
   */
  num = num + step;
  char letter = 'x';
  for (int i = 0;i < 100;i++) {
    if (i == 50) {
      a = b[i] / 5;
      info = " this is
             a";
    } else {
      a = a * letter;
    }
  }
  double c = 0x#
}
char y = 'x
return "a test";
/* and
